# Feature modules

Product UI belongs in auth, projects, nodes, resources, applications,
operations, agent, audit, or settings feature folders as those capabilities
arrive. Shared primitives belong under components/ui; API transport belongs
under lib.
