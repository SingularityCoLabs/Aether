# Provider contract tests

The shared provider lifecycle suite will live here when Docker arrives in
Phase 5. Every later provider must pass the same plan, apply, observe,
idempotency, and delete semantics.
