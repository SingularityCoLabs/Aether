# Cross-system E2E tests

The first full-system scenario will enroll one node, deploy nginx, observe
health and logs, restart it, and delete it. Browser-only Phase 0 coverage lives
under web/tests/e2e.
